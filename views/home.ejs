<%- include('partials/header.ejs') %>

 
    <h2>Open roles:</h2>
    <div class="jobsList">
      <% - categorias.forEach( (cat) => {%> 
      <h3><%-cat.categoria%></h3>
        <% - cat.vagas.forEach( (vaga) => {%>
          <ul>
            <li><a href="/vagas/<%-vaga.id%>"><%-vaga.titulo%></a></li>
          </ul>
        <%})%>
      <%})%>
    </div>
  </div>

 <%- include('partials/footer.ejs') %>